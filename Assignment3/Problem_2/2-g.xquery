<result>
{
	High Level Overview:

    For Each open auction with the name venkataash takano,
    fill in open_auction id and total bidders using count()
    return a subquery
    for each bidder_name in id(personref)
    return bidder_name
}
</result>
